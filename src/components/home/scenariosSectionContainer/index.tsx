import ScenariosSection from "./scenariosSection";

import styles from "./style.module.css";

const ScenariosSectionContainer = () => {
  return (
    <div className={styles.container}>
      <ScenariosSection />
    </div>
  );
};

export default ScenariosSectionContainer;
